{
  "name": "nextjs-ssr",
  "private": true,
  "workspaces": [
    "./host",
    "./remote1",
    "./remote2",
    "./remote3"
  ],
  "version": "0.0.0",
  "scripts": {
    "start": "concurrently \"cd host; npm run dev\" \"cd remote1; npm run dev\" \"cd remote2; npm run dev\" \"cd remote3; npm run dev\"",
    "build": "concurrently \"cd host; npm run build\" \"cd remote1; npm run build\" \"cd remote2; npm run build\" \"cd remote3; npm run build\"",
    "serve": "concurrently \"cd host; npm run start\" \"cd remote1; npm run start\" \"cd remote2; npm run start\" \"cd remote3; npm run start\"",
    "e2e:ci": "yarn start & wait-on http-get://localhost:3001/ && npx cypress run --config-file ../cypress/config/cypress.config.ts --config '{\"supportFile\": \"../cypress/support/e2e.ts\"}' --spec \"./e2e/*.cy.ts\" --browser=chrome"
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
